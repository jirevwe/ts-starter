apiVersion: v1
kind: Service
metadata:
  name: {{ include "getequity-template.fullname" . }}
  labels:
    {{- include "getequity-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "getequity-template.selectorLabels" . | nindent 4 }}
